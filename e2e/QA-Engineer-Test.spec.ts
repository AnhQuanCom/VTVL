import test, {expect} from "../fixtures/QAEngineerTestFixture"
import { Metamask } from "../pages/Metamask.pom";


test.describe('Offspring Performance @bas', () => {

    // test.beforeEach(async ({qAEngineerTestFixture}) => {
    //     await qAEngineerTestFixture.btnConnectionStatus.click()});

    test('VTVL-01-Verify Valid Signature Toast If User Input Right Wallet Address ', async ({qAEngineerTestFixture,context}) => {
      // await expect(qAEngineerTestFixture.btnConnectionStatus).toHaveText(/Connected!/);
      // await qAEngineerTestFixture.txtbxAddress.click();
      // await qAEngineerTestFixture.page.keyboard.press('Meta+KeyV');
      // await qAEngineerTestFixture.txtbxMessage.fill('Hello from Candidate');
      // await qAEngineerTestFixture.txtbxSignTheMessage.click();
      // // await qAEngineerTestFixture.page.keyboard.press('Control+Tab');
      // await qAEngineerTestFixture.page.mouse.click(50, 50);
      // // await qAEngineerTestFixture.txtbxMessage.focus();
      // await qAEngineerTestFixture.page.keyboard.press('Meta+KeyW');
      // await qAEngineerTestFixture.page.reload();
      // await qAEngineerTestFixture.btnSign.click();
      
    });

    // test('VTVL-02-Verify Valid Signature Toast If User Input Right Wallet Address', async ({qAEngineerTestFixture, context}) => {
      
    // });

      
      // let res = await qAEngineerTestFixture.getHorseWithOffspring(1)
      // if (!res) return
      // await qAEngineerTestFixture.divHorsePanel.click();
      // await qAEngineerTestFixture.cardOffsprings.waitFor();
      // const parent = await qAEngineerTestFixture.lblHorseHeader.innerText();
      // const [tabs] = await Promise.all([
      //   context.waitForEvent('page'),
      //   await qAEngineerTestFixture.cardOffsprings.click()
      // ])
      // await tabs.waitForLoadState()
      // const pages = context.pages()
      // await pages[1].bringToFront()
      // let breedingAndStud2 = await qAEngineerTestFixture.constructor(pages[1]);
      // await breedingAndStud2.cardParents.waitFor();
      // const parentsCount = await breedingAndStud2.cardParents.count();
      // let match = false
      // for (let i = 0; i < parentsCount ; i++) {
      //   res = await breedingAndStud2.cardParents.nth(i).innerText()
      //   if (res.includes(parent)) {
      //     match = true
      //     break
      //   }
      // }
      // await pages[1].close()
      // expect(match).toBeTruthy()    
  });
